CREATE TABLE reclamations (ID INTEGER NOT NULL, DESCRIPTION VARCHAR(255), TITRE VARCHAR(255), TYPEREC_ID INTEGER, ETUDIANT_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE typeDeReclamations (ID INTEGER NOT NULL, TYPE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE Utilisateurs (ID INTEGER NOT NULL, DTYPE VARCHAR(31), EMAIL VARCHAR(255), PASSWORD VARCHAR(255), ACTIF TINYINT(1) default 0, PRENOM VARCHAR(255), NOM VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE reclamations ADD CONSTRAINT FK_reclamations_ETUDIANT_ID FOREIGN KEY (ETUDIANT_ID) REFERENCES Utilisateurs (ID)
ALTER TABLE reclamations ADD CONSTRAINT FK_reclamations_TYPEREC_ID FOREIGN KEY (TYPEREC_ID) REFERENCES typeDeReclamations (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
